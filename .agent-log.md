# Drea — Agent Log

## Session 1 — 2026-04-15

### Phase 0 ✅
- Initialized git repo on `main` branch
- Created `README.md`, `.gitignore`, placeholder `index.html`, `netlify.toml`
- Installed `gh` via Homebrew, authenticated
- Created GitHub repo: https://github.com/scherrerandrea94-lgtm/drea
- Pushed initial commit to `main`
- Netlify connection left for Andrea to do manually

### Next
Phase 1a — App Shell, Navigation & Firebase

## Session 2 — 2026-04-16

### Phase 1a ✅
- Full PWA meta tags (viewport-fit=cover, apple-mobile-web-app-capable, theme-color)
- CDN scripts: React 18, ReactDOM 18, Babel standalone, Tailwind CSS
- Firebase compat SDK v10.12.0 initialized with real credentials (drea-fitness project)
- Tailwind config extended with full Drea color palette
- Global CSS: safe area insets, scrollbar hiding, fade-in animation, number spinner removal
- `useFirebase(path)` hook with .on('value') listener + cleanup
- `fbSet`, `fbUpdate`, `fbPush`, `fbRemove` helpers + `localGet`/`localSet`
- Three-tab nav (Today / History / You) with thin-stroke SVG icons
- Active tab: amber-gold color + dot indicator; inactive: muted gray
- Fade-in animation on tab switch, haptic feedback on tab press
- 480px max-width centered layout with safe area bottom padding
- `manifest.json` with standalone display, dark theme
- `sw.js`: versioned cache, skipWaiting, stale-while-revalidate for app assets, network-only for Firebase + Anthropic

### Next
Phase 1b — Exercise Database & Seeding

## Session 3 — 2026-04-16

### Phase 1b ✅
- `EXERCISE_MUSCLE_MAP` — 160+ exercises across chest, back, shoulders, biceps, triceps, traps, legs, core
- `ALL_EXERCISE_NAMES` sorted array derived from map keys
- `seedExerciseNames()` — one-time Firebase seed using `once('value')` check, pushes all names as keyed entries under `drea/exerciseNames`
- `SeedingScreen` — spinner + "Setting up your exercise library..." shown only on first-ever load
- `useFirebase('drea/exerciseNames')` live listener → sorted, deduped `exerciseNames` array passed down to tabs
- `ExerciseInput` component — autocomplete text input, filters up to 10 suggestions, tap to select, blur-safe (150ms delay), amber focus border, dark dropdown with hover states
- Falls back to `ALL_EXERCISE_NAMES` if Firebase hasn't loaded yet

### Next
Phase 2a — Split Picker & Workout Start Flow

## Session 4 — 2026-04-16

### Phase 2a ✅
- `SPLITS` constant — 5 splits (chest/back, bis/tris, shoulders, legs/abs, back/traps) + Free Workout
- `buildExerciseFrequency(workoutsData)` — counts exercise appearances across all workout history
- `getSuggestions(split, frequency)` — returns exercises for split's muscles sorted by user frequency, then fills with defaults; deduped
- `SplitIcon` component — unique thin-stroke SVG icon per split id (no emoji)
- `SplitPickerStep1` — split cards with icon + label + 3 preview exercises + chevron; Free Workout card has dashed border
- `SplitPickerStep2` — scrollable exercise toggle list; selected = amber border + filled checkbox; deselected = dimmed; sticky "Start Workout (N)" CTA
- `TodayHome` — "Ready to lift?" hero card with dumbbell icon and Start Workout button
- `TodayTab` — 3-state machine: home → pick_split → pick_exercises; Free Workout skips exercise selection and calls onStartWorkout directly
- `handleStartWorkout` stub in App — ready for Phase 2b to wire in the active logger
- `workoutsData` loaded from Firebase and passed to TodayTab for frequency computation

### Next
Phase 2b — Active Workout Logger

## Session 5 — 2026-04-16

### Phase 2b ✅
- `buildLastWeightMap(workoutsData)` — scans workouts newest-first, records highest weight per exercise for pre-fill
- `useElapsedMinutes(startTime)` — ticks every 15s, returns elapsed minutes
- `SetRow` — set number label, weight input (decimal/step 2.5), × separator, reps input (numeric), delete button; auto-focuses weight on add; amber border on focus
- `ExerciseBlock` — ExerciseInput autocomplete + remove button; set rows; "Add Set" button pre-fills weight from last set (or lastWeightMap); per-exercise volume display; new set auto-focused
- `ActiveWorkoutLogger` — header with "Logging Workout" + green pulse dot + elapsed time; scrollable exercise blocks; dashed "+ Add Exercise" button; notes textarea (500 char max); total volume hero display; sticky bottom bar with Cancel (gray, confirm dialog) + Finish Workout (amber-gold 2× width)
- `handleStartWorkout` in App — converts picker exercises to active format with lastWeightMap pre-fill; free workout starts with one blank exercise block; auto-switches to Today tab
- `handleCancelWorkout` — clears activeWorkout state
- `handleFinishWorkout` stub — clears state (Phase 2c will save to Firebase + detect PRs)
- `TodayTab` updated — shows ActiveWorkoutLogger when activeWorkout is set, otherwise shows home/picker state machine

### Phase 2c ✅
- `saveWorkout(exercises, notes, startTime, split)` — async, saves to `drea/workouts/{YYYY-MM-DD}`, updates `drea/stats` (totalGymDays, lastWorkoutDate), runs Epley PR detection vs `drea/personalRecords`, auto-adds new exercise names
- `epley(weight, reps)` Epley 1RM formula; `fbSafeKey(str)` Firebase key sanitizer; `formatDate(key)` timezone-safe date formatter
- `PRBanner` — amber glow animation, star icon, PR list with dismiss
- `TodaySummary` — expandable accordion with split, stats, full set details, notes, "Log Another Workout" button
- `handleFinishWorkout` in App — calls `saveWorkout`, clears `activeWorkout`, sets `newPRs`, haptic feedback
- `ActiveWorkoutLogger` — `saving` state disables Finish button during async save

### Next
Phase 3a — Workout History & Progression Charts

## Session 6 — 2026-04-16

### Phase 3a ✅
- `getWeekStart(dateStr)` — returns Sunday start of week for a date
- `computeWeeklyVolume(workoutsData, numWeeks)` — last N weeks with total volume per week
- `computeExerciseHistory(workoutsData, exerciseName)` — returns [{date, est1rm, weight, reps}] sorted ascending for a given exercise
- `ProgressionChart` — SVG line chart (est. 1RM over time): y-grid lines with labels, amber line + area fill, dots at data points, x-axis date labels (first/mid/last)
- `WeeklyVolumeChart` — SVG bar chart: 8 bars for last 8 weeks, amber bars, date labels, dark empty bars for zero weeks
- `WorkoutCard` — expandable accordion card: collapsed shows date/split/exercises/duration/volume; expanded shows full set details + notes
- `HistoryTab` — 3 sub-tabs (History | Progress | PRs):
  - History: sorted workout list (newest first), empty state with dumbbell icon
  - Progress: exercise picker dropdown (shows exercises with history), progression line chart, recent sessions grid table (last 5), weekly volume bar chart always visible
  - PRs: placeholder for Phase 3b
- App: `personalRecordsData` from Firebase passed to HistoryTab; `workoutsData` already loaded

### Phase 3b ✅
- `PRBoard` reusable component — loads spinner while null, empty state (star icon + encouragement), alphabetical list of PR cards
- Each PR card: amber border accent, exercise name (truncated with ellipsis), date achieved, weight × reps (amber-gold), est. 1RM (muted)
- Wired into History > PRs sub-tab (replacing placeholder)
- Wired into YouTab with "Personal Records" section header + record count
- `personalRecordsData` from Firebase now passed to both HistoryTab and YouTab

### Phase 4a ✅
- `BodyWeightChart` SVG: line + area fill (6% amber), dots, latest weight label, first/last x-axis dates, 2 y-grid lines
- `BodyWeightTracker` component (self-contained, `useFirebase('drea/bodyWeights')`):
  - Log form: decimal input + lbs/kg toggle (converts kg → lbs on save) + Log button; Enter key submits; validates 50–500 lbs / 22–227 kg
  - Button turns green and shows "Logged!" for 2.2s on success
  - Stats row (3 cards): Current weight, 7-Day Avg, Total Change (green if down, rose if up)
  - Trend chart shown when 2+ entries
  - Recent entries: last 10 newest-first, weight/date/delete button
  - Empty state: "Start tracking to see your trend."
- YouTab updated: Body Weight section above Personal Records, with uppercase section headers

### Phase 4b ✅
- `ExerciseManager` component — receives `exerciseNamesData` (raw keyed Firebase data) from App via YouTab
- Single add: text input (Enter submits), Add button, case-insensitive duplicate detection, toast feedback
- Bulk add: textarea (one per line), "Add All" button, deduplication with count of added vs skipped, batch Firebase update
- Toast: green for success, rose for warnings, auto-dismisses after 2.6s
- Search: live filter input above the list
- Exercise list: alphabetical, each row has name + subtle delete X (red on hover), deletes via `fbRemove`
- Mode toggle (Single / Bulk) with amber active state
- YouTab updated: Body Weight → Personal Records → Exercise Manager (with section headers)
- `exerciseNamesData` prop threaded from App → YouTab → ExerciseManager (reuses existing Firebase listener, no duplicate connection)

### Next
Phase 5a — Rest Timer
- `buildLastWeightMap(workoutsData)` — scans workouts newest-first, records highest weight per exercise for pre-fill
- `useElapsedMinutes(startTime)` — ticks every 15s, returns elapsed minutes
- `SetRow` — set number label, weight input (decimal/step 2.5), × separator, reps input (numeric), delete button; auto-focuses weight on add; amber border on focus
- `ExerciseBlock` — ExerciseInput autocomplete + remove button; set rows; "Add Set" button pre-fills weight from last set (or lastWeightMap); per-exercise volume display; new set auto-focused
- `ActiveWorkoutLogger` — header with "Logging Workout" + green pulse dot + elapsed time; scrollable exercise blocks; dashed "+ Add Exercise" button; notes textarea (500 char max); total volume hero display; sticky bottom bar with Cancel (gray, confirm dialog) + Finish Workout (amber-gold 2× width)
- `handleStartWorkout` in App — converts picker exercises to active format with lastWeightMap pre-fill; free workout starts with one blank exercise block; auto-switches to Today tab
- `handleCancelWorkout` — clears activeWorkout state
- `handleFinishWorkout` stub — clears state (Phase 2c will save to Firebase + detect PRs)
- `TodayTab` updated — shows ActiveWorkoutLogger when activeWorkout is set, otherwise shows home/picker state machine

### Next
Phase 2c — Finish Workout, PR Detection & Today Summary
